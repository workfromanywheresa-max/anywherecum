name: Trigger Worker

on:
  push:
    paths:
      - videos.json

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call Worker after delay
        run: |
          sleep 30
          curl -X POST https://anywherecumnotifications.workfromanywhere-sa.workers.dev/trigger
